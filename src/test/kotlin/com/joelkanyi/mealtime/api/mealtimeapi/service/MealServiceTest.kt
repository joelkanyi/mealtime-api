/*
package com.joelkanyi.mealtime.api.mealtimeapi.service

import io.mockk.every
import io.mockk.mockk
import io.mockk.verify
import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

class MealServiceTest {

    private val mealService: MealService = mockk(relaxed = true)

    @Test
    fun `should call its data source to retrieve meals`() {
        // When
        mealService.retrieveMeals()

        // Then
        verify(exactly = 1) { mealService.retrieveMeals() }
    }
}*/
